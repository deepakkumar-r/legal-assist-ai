# Contributing

Use Node 20+, create a focused branch, and run `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build` before opening a change. Add tests for observable behavior and never commit real client documents or secrets. Commits follow Conventional Commits, for example `feat: add grounded handoff export` or `fix: reject oversized uploads`.
